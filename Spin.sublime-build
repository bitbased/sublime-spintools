{
  "cmd": ["bstc", "${file}"],
  //"file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
  "working_dir": "${file_path}",
  //"selector": "source.spin",

  "variants":
  [
    {
      "name": "Run",
      "cmd": ["bstc", "${file}", "-p2", "-L", "${file_path}/lib/*.*"]
    }
  ]
}
